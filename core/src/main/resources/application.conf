akka {
  loglevel = INFO
  stdout-loglevel = INFO
  loggers = ["akka.event.Logging$DefaultLogger"]
  logger-startup-timeout = 30s

#  default-dispatcher {
#    fork-join-executor {
#      parallelism-min = 8
#    }
#  }
}

afka {
  bootstrap = [{host=localhost, port=9092}, {host=localhost, port=9093}]
}